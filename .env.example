# Configuration pour le déploiement Netlify
# Copiez ce fichier vers .env et remplissez les valeurs

# Base de données PostgreSQL (Neon recommandé pour Netlify)
DATABASE_URL=postgresql://username:password@host:port/database

# Secret pour les sessions (générez une clé sécurisée)
SESSION_SECRET=votre-cle-secrete-tres-longue-et-aleatoire

# Environnement (production pour Netlify)
NODE_ENV=production

# URL de base de l'application (sera remplacée par l'URL Netlify)
VITE_API_BASE_URL=https://votre-site.netlify.app